services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    healthcheck:
      test: [CMD, wget, -q, --spider, "http://localhost/"]
  db:
    image: postgres:16
    ports:
      - "5432:5432"
