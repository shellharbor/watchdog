settings:
  log_file: /tmp/watchdog-schema-test.log
  lock_file: /tmp/watchdog-schema-test.lock
  state_directory: /tmp/watchdog-schema-test-state
services:
  - name: broken
    check:
      type: command
      commands:
        - command: [true]
    only_if:
      - type: time_window
        days: Funday
        time: 09:00-10:00
